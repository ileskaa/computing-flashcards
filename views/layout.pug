doctype html
html(lang="en")
head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet" type="text/css" href="/static/style.css")
    link(rel="icon" type="image/x-icon" href="/static/img/card.ico")
    title Computing Flashcards

body
    div(id="loadOverlay" style="background-color:#333; position:absolute; top:0px; left:0px; width:100%; height:100%; z-index:99;")

    h1 
        i Computing Flashcards
    if name
        h2 Hello #{name}!
                form(action="/goodbye" method="post")
                    button(id="goodbye") Goodbye
    block content
    footer
        p An app to learn programming concepts.